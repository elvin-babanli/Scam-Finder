# TapLoop — local
OWNER_KEY="replace_with_a_long_random_secret"
STORAGE_DIR="./data"
IP_GEO_PROVIDER="ipapi"

# TapLoop — Render (with persistent disk mount at /var/data, see render.yaml)
# OWNER_KEY=<paste a long random secret in the Render dashboard>
# STORAGE_DIR=/var/data
# IP_GEO_PROVIDER=ipapi
