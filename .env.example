# TapLoop
# Owner secret for /results and /api/export — keep private.
OWNER_KEY="replace_with_a_long_random_secret"

# Visits append to: ${STORAGE_DIR}/visits.jsonl
STORAGE_DIR="./data"

# Optional: IP-based approximate location after consent only.
# Supported: ipapi
IP_GEO_PROVIDER="ipapi"
