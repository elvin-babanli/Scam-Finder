services:
  - type: web
    name: scam-finder
    env: node
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: OWNER_KEY
        sync: false
      - key: STORAGE_DIR
        value: ./data
      - key: IP_GEO_PROVIDER
        value: ipapi
